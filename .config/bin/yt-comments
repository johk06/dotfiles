#!/bin/sh

URL="$1"
FILE="${2:-comments.md}"
if [[ "$FILE" == "-" ]]; then
    FILE=/dev/stdout
fi

yt-dlp --progress --no-download --get-comments --dump-json "$URL" |
    yt-format-comments >"$FILE"
